<div class="content-wrapper">
    @(Html.Kendo().Grid<CML.Models.DispositionModel>()
              .Name("grid")
              .Columns(columns =>
              {
                  columns.Bound(c => c.Disposition);
                  columns.Command(command => { command.Edit(); command.Destroy(); }).Width(180);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create();
                  toolbar.Excel();
              })
              .Editable(editable => editable.Mode(GridEditMode.PopUp))
               .Pageable(pageable => pageable
                            .Refresh(true)
                            .PageSizes(true)
                            .ButtonCount(5))
              .HtmlAttributes(new { style = "height: 450px;" })
              .Scrollable()
              .DataSource(dataSource => dataSource
                  .Ajax()
                  .Model(model => model.Id(p => p.ID))
                  .Read(read => read.Action("CML_Disposition_Read", "Disposition"))
                  .Create(create => create.Action("CML_Disposition_Create", "Disposition"))
                  .Update(update => update.Action("CML_Disposition_Update", "Disposition"))
                  .Destroy(destroy => destroy.Action("CML_Disposition_Destroy", "Disposition"))
              )
    )
</div>