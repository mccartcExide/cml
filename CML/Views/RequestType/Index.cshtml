<div class="content-wrapper">
    @(Html.Kendo().Grid<CML.Models.RequestTypeModel>()
              .Name("grid")
              .Columns(columns =>
              {
                  columns.Bound(c => c.RequestType);
                  columns.Command(command => { command.Edit(); command.Destroy(); }).Width(180);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create();
                  toolbar.Excel();
              })
              .Editable(editable => editable.Mode(GridEditMode.PopUp))
              .Scrollable()
               .Pageable(pageable => pageable
                            .Refresh(true)
                            .PageSizes(true)
                            .ButtonCount(5))
              .HtmlAttributes(new { style = "height: 450px;" })
              .DataSource(dataSource => dataSource
                  .Ajax()
                  .Model(model => model.Id(p => p.ID))
                  .Read(read => read.Action("CML_RequestType_Read", "RequestType"))
                  .Create(create => create.Action("CML_RequestType_Create", "RequestType"))
                  .Update(update => update.Action("CML_RequestType_Update", "RequestType"))
                  .Destroy(destroy => destroy.Action("CML_RequestType_Destroy", "RequestType"))
              )

    )

</div>